<div class="container">
  <h1>退会フォーム</h1>
  <div>
    <p>退会する場合は「退会する」を押してください</p>
  </div>
  <div class="d-flex">
    <div>
      <%= link_to "退会する", out_user_path(@user), method: :patch, class: "btn btn-danger" %>
    </div>
    <div>
      <%= link_to "退会しない", user_path(@user), class: "btn btn-primary" %>
    </div>
    
  </div>
</div>